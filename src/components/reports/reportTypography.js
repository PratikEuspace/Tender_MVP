import { StyleSheet } from 'react-native';

/** Figma main_reports — section headings (Work Report, Export Report, etc.) */
export const reportSectionLabelStyle = {
  fontSize: 16,
  fontWeight: '700',
  color: '#111827',
  lineHeight: 20,
};

export const reportSectionLabelText = StyleSheet.create({
  label: reportSectionLabelStyle,
});
